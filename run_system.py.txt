from waitress import serve
from app import app, scheduler, create_tables # استيراد التطبيق والجدول الزمني
import threading

def run_server():
    # تشغيل السيرفر على الشبكة المحلية
    print("🚀 Center Management System is running on http://0.0.0.0:5000")
    serve(app, host='0.0.0.0', port=5000, threads=8)

if __name__ == '__main__':
    # 1. تهيئة قاعدة البيانات
    create_tables()
    
    # 2. تشغيل السيرفر
    run_server()